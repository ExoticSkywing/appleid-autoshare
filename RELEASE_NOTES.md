# v1.0.0

Apple ID AutoShare 的首个正式产品版本。该版本冻结了已经验收的新手/老玩家业务路径、移动优先教程、安全门槛、结果反馈、视觉界面、有界动效和交互提示音。

## 产品流程

- 新手模式：选择目标 → 人机验证 → 单账号披露 → 复制 Apple ID → 查看 App Store 真实截图并完成两道首次安全确认 → 复制密码 → 登录结果 → 目标应用下载结果 → 退出账号收尾或换号。
- 老玩家模式：极速出号、自由复制，成功立即结束，异常立即换号，账号耗尽进入店铺兜底。
- 页面后台/返回与会话恢复保留当前账号和进度；凭据不写入持久浏览器存储。
- 用户结果继续作为会话绑定的弱质量信号，不构成系统独立验证。

## UI与交互

- 深色、克制、精密的任务界面；白色主 CTA，蓝色只表示当前操作，低饱和橙表示注意，完成使用明确绿色。
- 移动端与桌面端使用独立烘焙的电影感 Beams 静态背景，正式运行时零 WebGL、零 Canvas、零视频背景循环。
- 角色选择、阶段切换、凭证出现、复制、结果、错误和完成使用短促有界动效；静止后无动效残留。
- 原生 Web Audio 合成交互提示音，首次用户手势后启用，支持持久静音、Voice 上限、自动释放和 Reduced Motion 静音契约。
- 图标与教程截图以真实高清资源呈现，敏感动作前可放大核验。

## 安全与后端

- Cloudflare Turnstile 服务端校验、HttpOnly 会话、Redis 单次 ticket、IP/Session 限流、同源请求约束。
- 双源独立采集、严格清洗、短时有效 slice、请求时聚合、会话内去重和反馈排序。
- 非 root、只读容器文件系统，提供健康与就绪探针。

## 正式验收基线

- 后端测试、Ruff、MyPy、JavaScript 语法和 Git whitespace gate 全部通过。
- WebKit 与 Firefox 覆盖声音生命周期与降级；WebKit 覆盖 393px 移动端和 1440px 桌面端。
- 新手有序门槛、老玩家自由路径、退出/换号、结果与成功、背景稳定通过浏览器回归。
- `/healthz`、`/readyz` 和正式域名可访问。
